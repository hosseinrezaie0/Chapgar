import os
import sys

sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))
from core.generator import create_pdf

# A comprehensive stress test containing all common Persian RTL issues
test_markup = """
= گزارش جامع تست چاپگر (Chapgar)

این یک پاراگراف برای بررسی نحوه نمایش متن‌های فارسی است. 
در اینجا کلماتی با نیم‌فاصله (ZWNJ) مانند «می‌رود»، «کتاب‌ها» و «دانش‌آموزان» وجود دارد که باید کاملاً چسبیده به هم و بدون فاصله اضافی نمایش داده شوند.

== ۱. تست ترکیب فارسی و انگلیسی (BiDi & UAX #9)
همان‌طور که می‌دانید، ابزار Typst بسیار قدرتمند است. 
متن‌های انگلیسی مثل (Open Source) و متغیرهای کد مثل `req.body.data` باید چپ‌به‌راست بمانند، اما کلمات فارسی اطراف آن‌ها نباید به هم بریزند.
همچنین این براکت‌ها [تست کروشه] و (تست پرانتز) باید در جهت درست (آینه‌ای) بسته شوند.

== ۲. تست تبدیل خودکار اعداد
تاریخ امروز: 1403/05/20
شماره تماس: 021-88888888
مبلغ پرداخت شده: 1,500,000 تومان
درصد پردازش: 95%

== ۳. تست آینه‌ای شدن لیست‌ها (Layout Mirroring)
- این اولین آیتم لیست است که باید بولت آن در سمت راست باشد.
- این آیتم شامل کلمه English است تا عدم به هم ریختگی را ثابت کند.
- آیتم سوم با شماره 3.14 در داخل متن.

+ مرحله اول: تنظیمات
+ مرحله دوم: (تست پرانتزها در لیست شماره‌دار)
+ پایان عملیات تست

== ۴. تست جداول راست‌به‌چپ
#table(
  columns: 3,
  fill: (x, y) => if y == 0 { luma(230) } else { none },
  [ردیف], [نام محصول], [قیمت (تومان)],
  [1], [لپ‌تاپ گیمینگ مدل (X-Pro)], [50,000,000],
  [2], [کیبورد مکانیکی], [3,500,000],
  [3], [موس بی‌سیم 2.4Ghz], [1,200,000]
)
"""

if __name__ == "__main__":
    print("Running Chapgar Stress Test...")
    output_path = create_pdf(test_markup, "chapgar_stress_test.pdf")
    print(f"Success! Open your PDF here: {output_path}")